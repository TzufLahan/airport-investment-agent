venv/
__pycache__/
.env
*.pyc

# Large raw BTS downloads — the small, curated snapshot in data/processed/ IS committed.
data/raw/

# Agent long-term memory is a runtime artifact, not source.
data/agent_memory.json

# Submission / build zips — never commit binaries into the repo.
*.zip
